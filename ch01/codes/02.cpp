#include<cstdio>
#include<cmath>
using namespace std;

int main() {
    double a, b, c;
    a = 3;
    b = 4;
    c = sqrt(a * a + b * b);
    printf("a = %lf, b = %lf, c = %lf", a, b, c);
    return 0;
}